export enum CourierStatus {
  AVAILABLE = 'AVAILABLE',
  BUSY = 'BUSY',
  OFFLINE = 'OFFLINE',
}

export enum CourierVehicleType {
  BIKE = 'BIKE',
  MOTO = 'MOTO',
  CAR = 'CAR',
}

export enum Entities {
  ORDER = 'order',
  COURIER = 'courier',
}
