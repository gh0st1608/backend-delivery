export enum ServerErrorMessages {
  DYNAMO_DB_ERROR = 'Conexion a dynamoDb fallido.',
}

export enum DomainErrorMessages {
  PRODUCT_NOT_FOUND = 'Producto no encontrado',
}

export enum DomainSuccessMessages {
  GET_PRODUCT_SUCCESS = 'Producto encontrado con exito',
  GET_PRODUCTS_SUCCESS = 'Productos encontrados con exito',
  CREATE_PRODUCT_SUCCESS = 'Producto creado con exito',
}
